import { ChargebackEmbed } from '../../data/chargebacks/Chargeback';
import { PaginationParameters } from '../../types/parameters';
export declare type ListParameters = PaginationParameters & {
    embed?: ChargebackEmbed[];
};
