import { SubscriptionData } from '../../../data/subscription/data';
import { PaginationParameters } from '../../../types/parameters';
import PickOptional from '../../../types/PickOptional';
interface ContextParameters {
    customerId: string;
    testmode?: boolean;
}
export declare type CreateParameters = ContextParameters & Pick<SubscriptionData, 'amount' | 'interval' | 'description' | 'mandateId'> & PickOptional<SubscriptionData, 'times' | 'startDate' | 'method' | 'webhookUrl' | 'metadata'>;
export declare type GetParameters = ContextParameters;
export declare type ListParameters = ContextParameters & PaginationParameters;
export declare type UpdateParameters = ContextParameters & Pick<SubscriptionData, 'mandateId'> & PickOptional<SubscriptionData, 'amount' | 'description' | 'interval' | 'metadata' | 'startDate' | 'times' | 'webhookUrl'>;
export declare type CancelParameters = ContextParameters;
export {};
